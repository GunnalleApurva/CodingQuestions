#include <cmath>
#include <cstdio>
#include <vector>
#include <iostream>
#include <algorithm>
using namespace std;


int main() {
   int n,a[100],i,sum=0;
    cin>>n;
    for(i=0;i<n;i++)
    {
        cin>>a[i];
        if( a[i] % 2 )
            sum = sum + a[i];
    }
    cout<<sum;
}
